import type { MapLayers } from '@/types';

export const DEFAULT_MAP_LAYERS: MapLayers = {
  fires: true,
  weather: false,
  dwi: false,
  satellites: true,
  flights: true,
  cctv: true,
  buildings: true,
};
